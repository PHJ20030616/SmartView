# SmartView Web

React + Ant Design 前端基础工程。前端只调用 Spring Boot 后端 Web API，不直接调用 FastAPI AI 服务。

## 本地启动

```bash
cd smartview-web
npm install
npm run dev
```

## 验证

```bash
npm run typecheck
npm run test
npm run build
```
